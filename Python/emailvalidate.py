# Create a file that contains number of email ids
# send a common email message to all emails
# verify the email address as well

filename = input("Enter file name ")
file = open(filename)

for i in file:
    

